# TerraformPractice

1. Download and install correct version of binary terrafor (https://www.terraform.io/downloads.html)
2. Move the binary file to a location within your $PATH in .bash_profile or other files.
3. Download this repo how you want to then cd to that directory
4. execute `terraform plan`
5. execute `terraform apply`
